# FocuSD 灵动岛功能说明

> 本项目基于开源项目 [FocuSD](https://github.com/zzliu93-debug/FocuSD) 的 MIT 许可证代码进行修改与重构。

## 1. 产品定位

FocuSD 灵动岛是运行在 Windows 桌面顶部的效率工具。它以透明、无边框、始终置顶的悬浮岛形式工作：平时收起，显示当前最需要关注的信息；点击后展开，处理待办、音乐、剪贴板、接口消息和外观设置。

## 2. 收起状态

- 默认岛屿标识为 `❤`，可在设置中替换为其他文字，或关闭显示。
- 默认收起宽度为 320 像素，可按需要调大。
- 未播放媒体时，默认显示金句：`心有山海，步履从容，自有风来。`
- 正在播放音乐且启用“播放时优先显示媒体”时，收起态显示歌曲或当前歌词。
- 外部接口消息到达时，优先展示消息；图片消息会显示缩略图。
- 长文本会在岛内截断或滚动，不会溢出到灵动岛外。

## 3. 顶部入口

展开灵动岛后，顶部彩色圆点从左到右对应：

- 蓝色：待办与每日记录。
- 紫色：系统媒体与歌词。
- 黄色：剪贴板历史。
- 粉色：接口扩展消息中心。
- 灰色：布局、外观和各项设置。

## 4. 待办、每日记录与专注

- 新增、编辑、完成、删除当天待办。
- 可设置当前专注任务，并启动专注倒计时。
- 可在设置中选择专注时长、是否允许拖动调整任务顺序、是否将未完成任务带入下一天。
- 支持每日记录、历史归档和卡片/时间线浏览。
- 设置 Markdown 保存目录后，可把当天待办与每日记录保存为本地 Markdown 文件。
- 专注结束后，岛屿会显示完成提示；系统允许时会发送 Windows 提醒。

## 5. 音乐与歌词

- 读取 Windows 系统媒体会话，显示歌曲名、作者、播放状态和当前播放时间。
- 支持上一首、播放/暂停、下一首。
- 支持在线歌词获取和逐行滚动；歌词会随播放时间自动定位当前行。
- 可在设置中关闭音乐页歌词显示。
- 系统媒体接口未提供统一可靠的歌曲总时长，因此当前只显示准确的播放时间，不提供不准确的拖动进度条。

## 6. 剪贴板历史

- 记录文本和图片剪贴板内容。
- 可设置是否记录、是否记录图片、最大保存条数。
- 每条记录可复制、收藏、备注和删除。
- 默认打开快捷键为 `Alt+Z`，可在设置中重新录制。
- 可开启“选择后自动粘贴回原窗口”：通过快捷键打开历史后，点选内容会复制、恢复原来的输入窗口并模拟粘贴。

## 7. 接口扩展与消息中心

应用在本机提供 HTTP 接口：`http://127.0.0.1:47821`。

支持四类消息：文字、图片、视频和语音。外部程序可发送 JSON：

```json
{
  "type": "image",
  "title": "图片提醒",
  "content": "点击查看详情",
  "url": "https://example.com/image.png",
  "actionUrl": "https://example.com",
  "duration": 12,
  "priority": "normal"
}
```

字段说明：

- `type`：`text`、`image`、`video` 或 `voice`。
- `title`：标题，最多 80 个字符。
- `content`：正文，最多 2000 个字符。
- `url`：图片、视频或语音地址；图片也支持 `data:image/...`。
- `actionUrl`：详情页中的“打开链接”目标，仅允许 HTTP/HTTPS。
- `duration`：接口消息收起态显示秒数，范围 3 到 60。
- `priority`：`normal` 或 `high`。高优先级可根据通知规则自动展开详情页。

消息中心最多保存最近 20 条接口消息，重启后仍可回看。支持：

- 左侧纵向标题列表和鼠标滚轮浏览。
- 标题、正文和来源搜索。
- 按文字、图片、视频、语音筛选。
- 固定重要消息。
- 删除当前消息或清空全部接口记录。
- 图片预览、视频播放、语音播放和跳转链接。

## 8. Windows 通知监听

应用可读取 Windows 通知中心中授权给它访问的通知来源，并用于调试或展示通知内容。

注意：只有真正进入 Windows 通知中心的消息才能被系统接口读取。某些软件不会发出 Windows 通知，或在系统/软件中关闭了通知，此时无法通过该能力获取内容。

## 9. 通知规则

在设置页的“通知规则”中可配置：

- 允许哪些接口消息类型上岛：文字、图片、视频、语音。
- 高优先级消息是否自动展开。
- 接口消息停留秒数，范围 3 到 60 秒。

关闭某一类型后，该类型的新接口消息不会出现在岛屿或接口历史中。

## 10. 外观预设

内置三个主题：

- 沉浸夜航：深色液态玻璃与红色点缀。
- 清透玻璃：高透明度、蓝色高光。
- 专注红心：经典深色样式、红色爱心标识和金句优先。

可保存当前设置为自定义预设、重命名、应用或删除自定义预设。内置预设不可删除，且启动时会自动去重。

## 11. 动效与材质控制

设置页的“布局设置”中提供以下控制：

- 玻璃强度和整体不透明度。
- 动效强度：控制收起态呼吸光和玻璃动态感。
- 展开速度：控制收起/展开切换速度。
- 边缘高光：控制液态玻璃边缘的反光强度。
- 阴影深度：控制岛屿离开桌面的悬浮层次。
- 展开圆角：控制展开卡片的圆润程度。
- 亮点颜色、岛屿背景和待办纸张颜色。

上述设置会随自定义预设保存，也会包含在数据备份中。系统开启“减少动态效果”时，应用会关闭非必要的呼吸动画。

## 12. 数据备份与恢复

设置页的“数据备份”可导出和导入 JSON 文件。

备份包含：

- 外观与通知规则。
- 自定义预设。
- 待办、每日记录、历史归档。
- 接口扩展消息记录。
- 剪贴板设置。

导入会覆盖备份中包含的对应数据。导入前建议先导出当前数据，保留一份回退副本。

## 13. 窗口与托盘

- 可拖动岛屿位置。
- 可收起、展开、贴边收起和恢复默认位置。
- 可最小化到系统托盘。
- 支持设置开机自动启动。

## 14. 本地数据与隐私

待办、设置、预设和接口消息记录默认保存在本机。接口仅监听 `127.0.0.1`，其他设备不能直接访问。

仍建议不要通过接口发送密码、令牌、身份证号等敏感信息；接口消息会被保存为最近记录，直到被自动挤出、手动删除或清空。

## 15. 常用命令

安装依赖：

```powershell
pnpm install
```

启动开发模式：

```powershell
pnpm tauri dev
```

生成 Windows 安装包：

```powershell
pnpm tauri build
```

只生成可执行文件、不生成安装包：

```powershell
pnpm tauri build --no-bundle
```
